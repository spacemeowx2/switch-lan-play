pipeline:
  build-docker-linux:
    image: docker
    commands:
      - docker build --rm -t switch-lan-play-build:linux -f docker/Dockerfile.linux .
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
  build-docker-all:
    image: docker
    commands:
      - docker build --rm -t switch-lan-play-build:all -f docker/Dockerfile.all .
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
  lan-play-linux:
    group: build-bin
    image: switch-lan-play-build:linux
    commands:
      - mkdir build.linux && cd build.linux
      - cmake ..
      - make
  lan-play-windows:
    group: build-bin
    image: switch-lan-play-build:all
    commands:
      - mkdir build.win && cd build.win
      - cmake -DCMAKE_TOOLCHAIN_FILE=../cmake/toolchain-mingw32.cmake ..
      - make
  server:
    group: build
    image: node:alpine
    commands:
      - cd server
      - npm install
      - npm run build
  github_pre_release:
    image: plugins/github-release
    secrets: [ github_token ]
    files:
      - build.linux/lan-play
      - build.win/lan-play.exe
    checksum:
       - sha1
    prerelease: true
    when:
      event: tag
